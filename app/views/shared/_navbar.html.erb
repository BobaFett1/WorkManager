<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand">WorkManager</a>
    </div>


    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to t('home'), root_path %></li>
        <% if user_signed_in? %>
        <li><%= image_tag current_user.avatar.url(:thumb) , :class => "round-image-50"%></li>
        <li class="dropdown">
          <a class="dropdown-to" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= t('manage') %><span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to t('customers'), customers_path %></li>
            <li><%= link_to t('work_steps'), work_steps_path %></li>
            <li><%= link_to t('projects'), projects_path %></li>
          </ul>
        </li>
        <% end %>
        <li><%= link_to t('about'), about_path %></li>
      </ul>

      <p class="navbar-text pull-right">
        <% if user_signed_in? %>
            <%= t('devise.sessions.signed_in_as') %> <strong><%= current_user.email %></strong>
            <%= link_to t('devise.sessions.edit_profile'), edit_user_registration_path, :class => 'navbar-link' %> |
            <%= link_to t('devise.sessions.log_out'), destroy_user_session_path, method: :delete, :class => 'navbar-link'  %>
        <% else %>
            <%= link_to t('devise.registrations.sign_up'), new_user_registration_path, :class => 'navbar-link'  %> |
            <%= link_to t('devise.sessions.log_in'), new_user_session_path, :class => 'navbar-link'  %>
        <% end %>
    </div>
  </div>
</nav>